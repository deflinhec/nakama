version: v1
name: buf.build/bcasino/wallet
deps:
  - buf.build/googleapis/googleapis
  - buf.build/grpc-ecosystem/grpc-gateway
breaking:
  use:
    - FILE
build:
  excludes:
    - payment/
lint:
  use:
    - DEFAULT
  except:
    - PACKAGE_DIRECTORY_MATCH
